CREATE TABLE equipos (
    id int NOT NULL AUTO_INCREMENT,
    nombre varchar(100) NOT NULL,
    pais varchar(25) NOT NULL,
    estrellas float NOT NULL,
    escudo varchar(225) NOT NULL,
    PRIMARY KEY (id)
);